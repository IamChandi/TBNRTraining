print("The maximum Int value is \(Int.max)")
print("The minimum Int value is \(Int.min)")
print("The maximum value of a 32 bit Int is \(Int32.max)")
print("The minimum value of a 32 bit Int is \(Int32.min)")

print("The maximum value of a 32 bit Int is \(UInt32.max)")
print("The minimum value of a 32 bit Int is \(UInt32.min)")

//let firstBadValue: UInt = -1
//let secBadValue: Int8 = 200

let y: Int8 = 120
let z = y &+ 10

let d1 = 1.1
let d2 : Double = 1.1
let f1 : Float = 100.3

if (d1==d2)
{ print("d1 is equal to d2")}
else { print("they aren't equal") }

print("d1 + 0.1 is \(d1+0.1)")
if d1 + 0.1 == 1.2 {print("they are equal")}
